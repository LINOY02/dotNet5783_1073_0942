<Window x:Class="PL.CatalogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        DataContext="{RelativeSource self}"
        mc:Ignorable="d"
        Title="CatalogWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key ="CatalogDataTemplate" DataType="local:ProductItem">
            <Border
                Background="{Binding InStock, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true, Converter={StaticResource}}">
                <Border.Effect>
                    <DropShadowEffect
                            ShadowDepth="1" />
                </Border.Effect>
               <StackPanel Width="210" Height="250">
                    <Image x:Name="prodImage"
                    Source="{Binding Path=picture, Converter={StaticResource ImagePathToBitmap}}"
                    HorizontalAlignment="Center"
                    Height="130"
                    Margin="15,15,15,15"
                    VerticalAlignment="Center"
                    Width="130" />
                    <TextBlock
                    Margin="5,5,5,5"
                    Text="{Binding Name}"
                    FontSize="15" />
                    <TextBlock
                    Margin="5"
                    Text="{Binding InStock, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"
                   FontSize="15" />
                    <TextBlock
                    Margin="5"
                    Text="{Binding Price,StringFormat={}{0:c}}"
                    FontSize="15" />
                    <TextBlock
                    Margin="5"
                    Text="{Binding Category}"
                    FontSize="15">
                        <TextBlock.Style>
                            <Style
                            TargetType="TextBlock">
                                <Setter
                                Property="Foreground"
                                Value="Blue"></Setter>
                                <Style.Triggers>
                                    <Trigger
                                    Property="IsMouseOver"
                                    Value="True">
                                        <Setter
                                        Property="Foreground"
                                        Value="Red" />
                                        <Setter
                                        Property="TextDecorations"
                                        Value="Underline" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                </Border>
        </DataTemplate>
    </Window.Resources>
    
        
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height ="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row ="0" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
             </Grid.ColumnDefinitions>
            <Label Name ="TabelLabel" Content="Tabels" Grid.Column="0" HorizontalAlignment="Center" />
            <Label Name ="ChairLabel" Content="Chairs" Grid.Column="1" HorizontalAlignment="Center"/>
            <Label Name ="SofaLabel" Content="Sofas" Grid.Column="2" HorizontalAlignment="Center"/>
            <Label Name ="ClosetLabel" Content="Closetss" Grid.Column="3" HorizontalAlignment="Center"/>
            <Label Name ="BedLabel" Content="Beds" Grid.Column="4" HorizontalAlignment="Center"/>
        </Grid>
        <ScrollViewer Grid.Row ="1">
            <ListView x:Name="CatalogListView"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                RenderTransformOrigin="0.5,0.5"
                ItemsSource="{Binding ProductItems}"
                ItemTemplate="{StaticResource CatalogDataTemplate}"
                Height="312"
                Width="716">
                <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
                </ListView>
        </ScrollViewer>

    </Grid>
</Window>
